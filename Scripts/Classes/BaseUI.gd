extends Control
class_name BaseUI

# Method to activate the UI
func activate() -> void:
    pass # Optional: Initialize or show UI elements

# Method to deactivate the UI
func deactivate() -> void:
    pass # Optional: Clean up or reset UI elements
